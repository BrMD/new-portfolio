/*
TODO:
//tem que mudar isso aqui
*/
type typeButtonHeader = {
  children: React.ReactNode;
};

type ChildrenWithoutProps = {
  children: React.ReactNode;
};

type TypeHOCpages = { option: string };
